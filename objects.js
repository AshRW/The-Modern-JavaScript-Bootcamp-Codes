let car = {
    brand:"Honda",
    model:"Accord",
    type:"Sedan",
    fuelConsumtion: 18
};

console.log(car);

console.clear();

console.log(`The Company ${car.brand} just launched ${car.model} as their first class ${car.type}. It has the lowest fuel consumption of ${car.fuelConsumtion}KM per litre`);