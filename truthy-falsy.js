// Truthy Values are non boolean values that correspond to the boolean true
// Falsy Values are non boolean values that correspond to the boolean false. 
//       ex: false, null, undefined, empty string(''), NaN

let a = '';

if(a){
    console.log("Truthy Value");
}else {
    console.log("Falsy Value");
}